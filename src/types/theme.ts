/** The fixed set of semantic accent tones components can be styled with — deliberately small so the UI reads as one calm system, not a rainbow. */
export type Tone = 'accent' | 'success' | 'warning' | 'danger' | 'neutral';
